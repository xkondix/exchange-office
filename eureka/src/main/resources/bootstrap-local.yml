spring:
  application:
    name: eureka

eureka:
  client:
    fetchRegistry: false
    registerWithEureka: false

management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 8761

